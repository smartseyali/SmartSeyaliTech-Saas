/**
 * usePrograms — Legacy hook, re-exports from useWebsite
 *
 * Kept for backward compatibility with existing pages.
 * New code should import directly from useWebsite.
 */
export { useItems as usePrograms, useItem as useProgram, useCategories } from "./useWebsite";
